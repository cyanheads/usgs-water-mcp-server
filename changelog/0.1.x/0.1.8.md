---
summary: "Validate NWIS inputs at the edge, bound and complete water_get_readings output, and adopt the mcp-ts-core 0.10.14 supply-chain baseline."
breaking: false
security: false
---

# 0.1.8 — 2026-07-15

## Added

- **`water_get_readings` `totalValues` / `truncated`** — each site+parameter series is capped at its 10 most recent records (`VALUES_PER_SERIES_CAP`) in the handler, so the cap now reaches `structuredContent` rather than existing only in rendered text; `totalValues` reports how many records the period actually held upstream, `truncated` flags when a series was capped. `format()` derives its slice and message from the same cap instead of a separately hardcoded preview count. ([#10](https://github.com/cyanheads/usgs-water-mcp-server/issues/10))
- **`water_get_readings` `missingSites`** — requested site numbers NWIS returned no series for are diffed out and reported here, rendered in `format()` with a pointer to `water_find_sites`, instead of being dropped from the batch without comment. ([#11](https://github.com/cyanheads/usgs-water-mcp-server/issues/11))
- **`.gitattributes`, `.github/SECURITY.md`, `.github/FUNDING.yml`** — LF normalization and `linguist-generated` markers for `bun.lock`/`CHANGELOG.md`/`docs/tree.md`; a security policy directing reports to GitHub's private disclosure flow or `casey@caseyjhand.com`; GitHub Sponsors and Buy Me a Coffee funding links.
- **`bunfig.toml` supply-chain guards** — `minimumReleaseAge = 259200` (3 days) blocks freshly-published package versions, with `@cyanheads/mcp-ts-core` excluded so framework releases still adopt same-day; `[install.security] scanner = "@socketsecurity/bun-security-scanner"` scans every install for malware, typosquats, and known CVEs.

## Changed

- **Dockerfile** — drops the `apt-get python3 make g++` toolchain stage; DuckDB's native binding arrives as a prebuilt os/cpu-gated platform package, so both stages install with `--ignore-scripts` and neither needs a compiler. Base image pinned `oven/bun:1.3` → `oven/bun:1.3.14` in both stages; both install steps gain a BuildKit cache mount (`--mount=type=cache,target=/root/.bun/install/cache`).
- **`devcheck`'s outdated-dependency check** — a reported row is now skipped only when it's allowlisted or held back by `bunfig.toml`'s `minimumReleaseAge` guard (`bun outdated`'s `*` marker); a genuine available update still fails the check.

## Fixed

- **NWIS input validation and error attribution** — `src/services/nwis/input-schemas.ts` (new) holds Zod patterns for the formats NWIS actually accepts (site number, parameter code, ISO 8601 period, HUC, county FIPS, state, bbox), shared by every tool that forwards a value upstream; the patterns serialize into each tool's advertised JSON Schema as `pattern`, so malformed input is rejected before the call instead of reaching NWIS. `classifyNwisFailure()` (new, in `nwis-service.ts`) then branches on the error's own type (`validationError` → `invalid_request`, `serviceUnavailable` → `upstream_error`) rather than substring-matching its message: `water_find_sites`'s `invalid_filter` and `water_get_readings`'s `invalid_site_format` collapse into a shared `invalid_request` carrying NWIS's own message, which names the field it actually rejected — previously a malformed `period` could surface as a bad site number. `water_get_series` keeps `invalid_date_range` for its own handler-owned calendar/ordering checks, which never reach NWIS. ([#10](https://github.com/cyanheads/usgs-water-mcp-server/issues/10), [#11](https://github.com/cyanheads/usgs-water-mcp-server/issues/11), [#12](https://github.com/cyanheads/usgs-water-mcp-server/issues/12))
- **`huc` / `countyCd` `.describe()` text** — `huc` no longer claims NWIS accepts "2, 4, 6, or 8 digits" (only 2 and 8 are valid); `countyCd`'s example moves off the rejected colon form (`51:013`) to the accepted bare 5-digit FIPS code (`51013`). ([#12](https://github.com/cyanheads/usgs-water-mcp-server/issues/12))

## Dependencies

- `@cyanheads/mcp-ts-core` ^0.10.9 → ^0.10.14
- `@types/node` ^26.0.0 → ^26.1.1
- `ignore` ^7.0.5 → ^7.0.6
- `vitest` ^4.1.9 → ^4.1.10
- New devDependency: `@socketsecurity/bun-security-scanner` ^1.1.2 (added to `trustedDependencies`)
- `bun audit` clears all 7 previously-flagged advisories via the bumps above (0 remaining)
